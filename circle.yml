machine:
  timezone: UTC

  php:
    version:
      5.6.23

  hosts:
    yuki.nit2a.com: 127.0.0.1

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - composer install
    - npm install
    - bower install

  cache_directories:
    - ~/.composer/cache/

  override:
    - npm run gulp compile-prod

test:
  pre:
    - echo "CI Test Start"
  pre:
    - echo "CI Test End"
